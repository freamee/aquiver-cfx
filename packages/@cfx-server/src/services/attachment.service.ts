import { Vector3 } from 'three';
import { NetEntity } from '..';

export class AttachmentService {
	add(entity: NetEntity, id: string, offsetPos: Vector3, offsetRot: Vector3) {
		//
	}

	remove(entity: NetEntity, id: string) {
		//
	}

	has(entity: NetEntity, id: string) {
		//
	}
}
